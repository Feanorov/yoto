Font files used by the video generator are cached in this directory.

At runtime the font loader will first look here for DejaVu Sans / Noto Sans files,
then copy a Cyrillic-capable system font here if the directory is empty.
